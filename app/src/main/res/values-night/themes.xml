<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PocketAssistant" parent="Theme.MaterialComponents.DayNight.DarkActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/skin</item>
        <item name="colorPrimaryVariant">@color/skin_dark</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorOnBackground">@color/dark_background</item>
        <item name="colorAccent">@color/gold</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/text_dark</item>
        <item name="android:titleTextColor">@color/text_disabled</item>
        <item name="titleTextColor">@color/text_disabled</item>
        <item name="background">@color/gold</item>

    </style>
</resources>